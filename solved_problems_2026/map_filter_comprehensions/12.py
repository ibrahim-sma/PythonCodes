"""
12. Word Length Grouping
Concepts: dictionary comprehension, list comprehension
📌 Task:
Given a list of words, group them into a dictionary where
key = word length, value = list of words of that length.
"""

input_list = "from a list of integers, filter positive numbers and calculate their sum".split(" ")
word_length_list = [len(word) for word in 

print(input_list)
word_length_dict = {
    length: word for 
}
